<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ldxx.xxbase" >

    <!-- 请求网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读写sd卡权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".demo.activity.WelcomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".demo.activity.DemoBActivity"
            android:label="@string/title_activity_demo_b" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_demo_main" />

        <service
            android:name=".demo.service.LoadDataIntentService"
            android:exported="false" >
        </service>

        <activity
            android:name=".demo.activity.DemoMainActivity"
            android:label="@string/title_activity_welcome" >
        </activity>
        <activity android:name=".demo.activity.DemoAppBarLayoutActivity" />
        <activity android:name=".demo.activity.DemoTextInputLayoutActivity" />
        <activity
            android:name=".demo.activity.DemoCustomViewActivity"
            android:label="@string/title_activity_demo_custom_view" >
        </activity>
    </application>

</manifest>
